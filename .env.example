# Server
PORT=3000

# PostgreSQL (Prisma)
DATABASE_URL="prisma+postgres://user:password@host:port/dbname?connection_limit=5"

# Redis (optional — falls back to localhost:6379)
REDIS_URL="redis://localhost:6379"
